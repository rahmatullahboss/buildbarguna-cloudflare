---
name: typescript-react
description: "Typescript React for buildbarguna-cloudflare. 1 fixes."
domain: typescript-react
composesFrom:
  - react
triggers:
  - glob: "**/*.tsx"
autoGenerated: true
enabled: true
---

# Typescript React

Auto-compiled from **11 real patterns** in **buildbarguna-cloudflare**. This skill is auto-routed to agents when working on typescript-react files.

## 🔧 Problem Playbooks

### problem-fix in ImageUpload.tsx
File updated (external): buildbarguna-cloudflare/frontend/src/components/ImageUpload.tsx

Content summary (183 lines):
import { useState, useRef } from 'react'
import { Upload, X, ImageIcon, Loader2, CheckCircle } from 'lucide-react'
import { compressImage, formatBytes } from '../lib/imageCompress'
import { getToken } from '../lib/apiToken'

interface ImageUploadProps {
  value: string
  onChange:

**Actionable Steps:**
1. Modified 1 files

## 🔵 Architecture

### how-it-works in Withdraw.tsx
-           <button onClick={() => setErrMsg('')} className="ml-auto">✕</button>
+           <button onClick={() => setErrMsg('')} className="ml-auto" aria-label="ত্রুটি বার্তা বন্ধ করুন">✕</button>



## 🤔 Decisions & Trade-offs

- **decision in ProfitDistribution.tsx** — -             <button onClick={() => navigate(-1)} className="bg-white/15 p-2 rounded-xl hover:bg-wh
- **decision in ProjectDetail.tsx** — -       <button onClick={() => navigate(-1)} className="text-sm text-gray-500 hover:text-primary-600
- **decision in EditProfile.tsx** — -           className="w-full py-3 mt-4 text-gray-600 hover:text-gray-800 text-sm"
+           aria-

---
*Auto-generated by BrainSync 🧠 | 11 patterns | 2026-03-23*
